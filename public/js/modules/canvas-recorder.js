import { logToConsole } from './logger.js';

// mainMediaRecorder, mainRecordedChunks, isMainCanvasRecording, appConfig, fetchAppConfig, and initializeCanvasRecorder are removed.
// If other canvas recording functionalities exist in this file, they would remain.
// For now, this file will be nearly empty if initializeCanvasRecorder was the only major function.

// Example: Keeping other potential (unrelated) exports if they existed.
// export function someOtherCanvasFunction() { /* ... */ } 